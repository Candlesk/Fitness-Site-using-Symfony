{% extends 'base.html.twig' %}

{% block title %}Diet{% endblock %}

{% block body %}
    <h1>Diet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ diet.id }}</td>
            </tr>
            <tr>
                <th>Calories</th>
                <td>{{ diet.Calories }}</td>
            </tr>
            <tr>
                <th>Fat</th>
                <td>{{ diet.Fat }}</td>
            </tr>
            <tr>
                <th>Carbohydrates</th>
                <td>{{ diet.Carbohydrates }}</td>
            </tr>
            <tr>
                <th>Proteins</th>
                <td>{{ diet.Proteins }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_diet_index') }}">back to list</a>

    <a href="{{ path('app_diet_edit', {'id': diet.id}) }}">edit</a>

    {{ include('diet/_delete_form.html.twig') }}
{% endblock %}
